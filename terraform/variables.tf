variable "project_id" {
  description = "The Google Cloud project that holds the Maps Grounding Lite API enablement and the restricted API key. A billing account must be linked (Maps Platform requires one even inside the free monthly cap)."
  type        = string
}
