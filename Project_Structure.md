# Project Structure: Headless

Functional map of the codebase. Read this before proposing any change; log every file
addition, move, or removal in the Changelog table immediately (`scripts/verify_structure.py`
enforces it).

## Director Layer

| Path | Purpose |
| :--- | :--- |
| `CLAUDE.md` | **Constitution**: roles, the five core lessons (AVF-derived), and the Headless hard rules (gates, secrets, browser, working style). Wins on any conflict. |
| `PATTERNS.md` | **Pattern Registry**: established engineering patterns and design decisions. |
| `Project_Structure.md` | **Architecture Map**: this document, including the Changelog. |
| `Function_Mapping.md` | **Errand Map**: each errand script mapped to its site, reads, writes-up-to, secrets, and handoff point. |
| `MEMORY.md` | **Operating ledger**: identity and environment, known site traps, "Errands run" table, session ids, open items. Read at session start. |
| `README.md` | Setup and usage. |
| `LICENSE` | MIT. |
| `.gitignore` | Excludes secrets, the virtualenv, previews, and the Chrome profile. |
| `.env.example` | Template for the non-secret configuration in `.env`. |
| `requirements.txt` | Python dependencies (Playwright, python-dotenv, pytest). |
| `scripts/` | **Agentic Skills**: maintenance scripts and errand scripts (see `scripts/README.md`). |
| `terraform/` | **Infrastructure-as-Code**: the GCP Secret Manager project, cost-gated (README only until the project is created). |
| `.specify/` | **Spec Kit Core**: constitution distillation (`memory/constitution.md`), templates, Python and bash helper scripts, workflow registry. Mapped at directory level. |
| `.claude/` | **Claude Code Integration**: Spec Kit skills (`/speckit-*`) plus the framework-generic `prototype` and `skill-distill` skills inherited from AVF; `settings.json` (v0.0.2) registers the commit safety gate's `PreToolUse` write-time check (`scripts/scan_secrets.py --stdin-hook`). Mapped at directory level. |
| `.githooks/` | **Commit Safety Gate** (v0.0.2): `pre-commit` (POSIX `sh`) runs `scripts/scan_secrets.py --staged`, refusing a commit that would introduce a credential or personal identifier. Activated per clone via `git config core.hooksPath .githooks` (README Setup step; `scripts/check_env.py`'s `git_hooks` row). |
| `.github/` | **CI** (v0.0.2): `workflows/secret-scan.yml` runs `scripts/scan_secrets.py --history`, the unit suite, and `verify_structure.py` on every push and pull request, alongside a `gitleaks` job, both free for a public repository. |
| `.scanignore` | **Commit Safety Gate allowlist** (v0.0.2): known-safe values exempted from the scanner, one exact string or `re:` pattern per line; an inline `# scan:allow` marker exempts a single line without a repository-wide entry. |
| `specs/` | **Feature Specs**: durable per-feature artifacts (`NNN-slug/spec.md`, `plan.md`, `tasks.md`, ...). Mapped at directory level. |

## Application Layer

| Path | Purpose |
| :--- | :--- |
| `headless/` | Reusable package: `config.py` (env and paths), `session.py` (headed persistent Chrome via Playwright or CDP attach), `secrets.py` (Keychain / GCP Secret Manager seam), `profile.py` (registry of typeable values), `gates.py` (preview / apply / check modes and the human handoff), `fields.py` (`FieldPlan`, `Source`, `redact`, `resolve_source`), `preview.py` (redacted screenshot + field-diff artifacts), `errand.py` (`Errand` base class and the run state machine). |
| `scripts/check_env.py` | Environment self-test: Chrome, profile dir, Playwright, secrets backend, `core.hooksPath` activation (v0.0.2). |
| `scripts/probe.py` | First errand: open a URL in the Headless profile and write a preview artifact. |
| `scripts/scan_secrets.py` | Commit safety gate (v0.0.2): credential/PII scanner, standard library only. `--staged`/`--paths`/`--history`/`--stdin-hook`. Not a browser errand. |
| `tests/` | `pytest` unit tests for pure logic, plus an opt-in browser suite (`test_gates_browser.py`, `HEADLESS_TEST_BROWSER=1`) against `tests/fixtures/form.html`. |
| `previews/` | Redacted preview artifacts written at runtime. Gitignored; absent until first run. |
| `requirements-gcp.txt` | Optional extra: `google-cloud-secret-manager`, imported lazily by `headless/secrets.py` only when the `gcp` backend is selected. Not part of `requirements.txt`. |

## Changelog

| Date | Action | Files Affected | Summary |
| :--- | :--- | :--- | :--- |
| 2026-08-24 | INITIALIZE | `CLAUDE.md`, `PATTERNS.md`, `Project_Structure.md`, `Function_Mapping.md`, `MEMORY.md`, `README.md`, `LICENSE`, `.gitignore`, `.env.example`, `requirements.txt`, `scripts/README.md`, `scripts/verify_structure.py`, `terraform/README.md` | **V0.0.0 Template Baseline.** Director layer materialized from Agentic-Vibe-Fleet (V0.0.7) for a Claude-led personal browser-errand runner: constitution of record is `CLAUDE.md` (five lessons adapted; Bruno gate replaced by errand validation), pattern registry seeded from the Director's Atlassian toolkit conventions (thin package, one script per errand, preview-by-default, registry-only writes). GitHub Spec Kit 1.0.2 initialized with the Claude integration and Python scripts; constitution distillation seeded from this repo's own `CLAUDE.md`. AVF's Gemini-only LLM scripts, `.gemini/`, and `bruno/` deliberately not carried over. `verify_structure.py` exclusions extended for `.venv/`, `previews/`, `tests` caches. Cost review: $0, no infra. |
| 2026-08-24 | ADD | `headless/__init__.py`, `headless/config.py`, `headless/gates.py`, `headless/fields.py`, `headless/preview.py`, `headless/secrets.py`, `headless/profile.py`, `headless/session.py`, `headless/errand.py`, `requirements-gcp.txt`, `scripts/check_env.py`, `scripts/probe.py`, `tests/conftest.py`, `tests/fixtures/form.html`, `tests/test_config.py`, `tests/test_gates.py`, `tests/test_fields.py`, `tests/test_preview.py`, `tests/test_secrets.py`, `tests/test_profile.py`, `tests/test_errand.py`, `tests/test_gates_browser.py` | **V0.0.1 Foundation Errand Runner** (spec `001-foundation-errand-runner`). Delivered the shared mechanics every errand composes: non-secret configuration (`config.py`), the preview/apply/check gates with no submit/pay/verify/otp path anywhere (`gates.py`), the secrets vault seam with Keychain default and a lazily-imported GCP backend (`secrets.py`), the profile registry as the only typeable source (`profile.py`), typed field plans and redaction (`fields.py`), redacted preview artifacts masked at construction (`preview.py`), the headed persistent-profile Chrome session (`session.py`), and the `Errand` base class wiring argparse to the run state machine (`errand.py`). Shipped two errands: `check_env` (four-row self-test, opens no browser window) and `probe` (open a URL, seed logins, write a preview). Browser-free unit suite (92 tests, under 1 s) plus an opt-in fixture-page browser suite (`HEADLESS_TEST_BROWSER=1`, 6 tests) both green; `verify_structure.py` SUCCESS. Cost review: $0, no infra (GCP backend code-only, not activated). Hardened post-verification (same version): `FillFailed` wraps every `Session.fill` locator error so a raw typed value can never reach stdout/stderr; screenshots are masked (form-control text) before capture; `--no-screenshot`/`HEADLESS_SCREENSHOTS=0`; plan sources pre-resolve before any window opens in every mode, not just apply; CDP attach always opens its own tab and disconnects without killing the Director's Chrome; `preview_dir` resolves against the repo root, not cwd; `check_env.py` moved to Maintenance (not an errand) with its own argument parser; a structural test (`tests/test_no_direct_typing.py`) enforces that no script types outside `Session.fill`. New files logged below. |
| 2026-08-24 | ADD | `tests/test_session.py`, `tests/test_check_env.py`, `tests/test_no_direct_typing.py` | **V0.0.1 hardening pass** (same feature, post-verification fixes; see the row above). Three new test modules: `test_session.py` (screenshot masking via a stub page; `Session.fill` refused outside apply; the headless-apply constructor refusal, all browser-free), `test_check_env.py` (unknown-flag refusal), `test_no_direct_typing.py` (structural scan of `scripts/*.py`). |
| 2026-08-24 | ADD | `tests/fixtures/csp.html` | **V0.0.1 second hardening pass** (same feature, further re-verification fixes). `Session.screenshot()` now degrades gracefully when a page's CSP blocks the mask's `<style>` injection: returns `None` (JSON-only artifact) and prints one note, never falls back to an unmasked capture; `_SCREENSHOT_MASK_CSS` also covers `[contenteditable]`. `headless/config.py`'s `preview_dir` now rejects any relative `HEADLESS_PREVIEW_DIR`/`--preview-dir` other than the literal default `previews` (`ConfigError`), instead of silently resolving it against the repo root. `headless/profile.py` gained `ProfileError(ValueError)` (position-only message) so `Errand.run`'s pre-session handler narrows its safe-to-print exception list from any `ValueError` to `ProfileError` specifically. `tests/test_no_direct_typing.py` rewritten from a line regex to an `ast.Call` walk (docstrings/comments can no longer false-positive). `tests/fixtures/csp.html` is the new fixture page (`<meta http-equiv="Content-Security-Policy" content="style-src 'self'">`) driving the CSP browser test; `tests/fixtures/form.html` gained a `#notes` `[contenteditable]` field (no new file, already logged). Browser-free unit suite now 96 tests under 1 s; opt-in browser suite now 7 tests. |
| 2026-08-24 | UPDATE | `headless/config.py`, `headless/gates.py`, `headless/session.py`, `headless/errand.py`, `scripts/probe.py`, `tests/test_config.py`, `tests/test_session.py`, `CLAUDE.md`, `.specify/memory/constitution.md`, `PATTERNS.md`, `.env.example`, `README.md`, `specs/001-foundation-errand-runner/spec.md`, `specs/001-foundation-errand-runner/data-model.md`, `specs/001-foundation-errand-runner/contracts/cli-and-package.md` | **V0.0.1 "quiet by default" (Director decision 2026-08-24).** No new files. A visible Chrome window popping up during test/smoke/errand runs was interfering with the Director's work, so Headless is now invisible unless a window is actually needed. Two independent `Config` axes: `headed` (can a real windowed Chrome be produced - `HEADLESS_HEADED`/`--headless`, unchanged meaning) and `show` (is the window visible from launch - `HEADLESS_SHOW`/`--show`, new, default `False`). Preview and check now launch Chrome's own headless mode by default, ignoring `HEADLESS_HEADED` entirely, unless `--show`. Apply still always opens a real window (the handoff needs one) but hides it immediately after launch and restores/surfaces it only at `handoff()`, right before "Your turn" - never on the CDP-attach path, which must never touch the Director's own browser window. `--headless`/`--show` are now their own mutually exclusive flag group; `--headless` with `--apply` is still refused. Empirically verified on this machine that hiding is best-effort, not a hard guarantee: neither CDP's reported `windowState: "minimized"`, the `--start-minimized` launch arg, nor `--window-position=-32000,-32000` had any real effect; `osascript` native minimize needs a one-time interactive Accessibility grant this session could not make; what worked partially was a post-launch `Browser.setWindowBounds` position push towards the screen's bottom-right corner, which macOS clamps to a small sliver rather than eliminating entirely - see `PATTERNS.md`. `constitution.md` bumped to 1.1.0 (MINOR: materially changed Browser guidance). Commit gate stays green: 112 unit tests (up from 96) under 1 s, opt-in browser suite (still fully headless) 7 tests. |
| 2026-08-24 | ADD | `scripts/scan_secrets.py`, `.githooks/pre-commit`, `.github/workflows/secret-scan.yml`, `.scanignore`, `.claude/settings.json`, `tests/test_scan_secrets.py` | **V0.0.2 Commit Safety Gate** (spec `002-commit-safety-gate`). The repository went public 2026-08-24; every future commit is now scanned for credentials and personal identifiers at three points. `scripts/scan_secrets.py`: standard-library-only Python (verified under both the project `.venv` and the macOS system `python3` 3.9.6), four mutually exclusive modes (`--staged`, `--paths`, `--history`, `--stdin-hook`), 18 named patterns (credential shapes plus PAN/Aadhaar/phone/email/card/IBAN; 15 at first ship, +3 in the 2026-08-25 hardening pass below), masking at construction (`"****" + value[-2:]`, never a raw value in any output), `.scanignore` allowlist (exact string or `re:` entry) plus an inline `# scan:allow` marker. `.githooks/pre-commit` (POSIX `sh`, executable bit staged) runs `--staged`, locates the repo root via `git rev-parse --show-toplevel`, propagates the scanner's exit code; activated per clone via `git config core.hooksPath .githooks` (new README Setup step; `scripts/check_env.py` gained a fifth row, `git_hooks`). `.claude/settings.json` registers a `PreToolUse` hook (`--stdin-hook`) on `Write|Edit|MultiEdit|NotebookEdit`, denying via a `hookSpecificOutput`/`permissionDecision` JSON object on stdout (always exit `0`, fail-open on anything unparseable) - the same convention as the global `no-em-dash.py` hook, not the exit-code shape originally sketched for this mode. `.github/workflows/secret-scan.yml` runs `--history` (every blob reachable from `HEAD`, deduped by blob sha, two subprocess calls total) plus the unit suite plus `verify_structure.py` on `ubuntu-latest`/Python 3.12, alongside a second `gitleaks/gitleaks-action@v2` job, both free for this public repository; GitHub's own secret scanning and push protection (already enabled 2026-08-24) run independently of both. `--history` against this repository's own real history found four legitimate false positives in pre-existing v0.0.1 content (a template hash, a bash int64-max constant, a Python f-string) - each added to `.scanignore` as its own value, no pattern weakened; see `PATTERNS.md`. Commit gate updated: `CLAUDE.md` Lesson 4 and `constitution.md` Principle IV/Hard Rules (bumped 1.1.0 -> 1.2.0, MINOR) now name `scan_secrets.py --staged` alongside `pytest`/`verify_structure.py`. Unit suite: 160 passed, 7 skipped (opt-in browser suite) in about 2.5 s. `--history` on the real repo: exit 0, well under the 2 s budget. See the 2026-08-25 row below for the post-implementation hardening pass that superseded the pattern count, the `.scanignore` false-positive handling, and the unit suite numbers in this row. |
| 2026-08-25 | UPDATE | `scripts/scan_secrets.py`, `.scanignore`, `.claude/settings.json`, `tests/test_scan_secrets.py`, `PATTERNS.md`, `Project_Structure.md`, `specs/002-commit-safety-gate/{research.md,data-model.md,contracts/cli-and-hooks.md,quickstart.md}` | **V0.0.2 Commit Safety Gate, post-implementation hardening pass** (15 findings from an adversarial review, applied as a verified fix batch; no new files). `scan_secrets.py`: every Finding on a line now shares one masked snippet built from every matched value on that line (longest first), capped to 200 characters around the first match, instead of each Finding masking only its own value and leaking its neighbors (a second detected secret, or an undetected one outside the cap window) raw; `--staged` diff parsing is now a real unified-diff state machine (`diff --git `/`@@` only ever start a section in the right state, `+++ `/`--- ` are headers only right after `diff --git `, and once a hunk header is seen every `+`-prefixed line is content no matter what it says), closing a content-as-diff-header bypass (a line reading `++ b/previews/notes.md` used to become `+++ b/previews/notes.md` and be mistaken for a real file header, silently swallowing every following line) and adding git-C-quote path unquoting; the staged-diff subprocess no longer uses `text=True` (a non-UTF-8 staged byte used to crash the parse with `UnicodeDecodeError`, leaving the whole diff unscanned); `EMAIL_RE`'s quantifiers are now bounded and guarded by an `"@" not in line` check (was unbounded and quadratic - 11+ seconds on a single 100 KB line, past the Claude Code hook's 10 s timeout); `phone_us`/`phone_in`/`payment_card` gained a `(?<![0-9])`/`(?![0-9])` digit-adjacency boundary and `aadhaar_in`/`iban` gained checksum validation (Verhoeff, mod-97) so a shape-only match inside an unrelated digit run, hash, or int64 constant can no longer fire; `generic_secret_assignment` now requires no embedded quote or comma in the captured value and exempts placeholder-shaped captures (`${...}`, `<...>`, `changeme`, and similar); three patterns added (`github_pat`, `google_oauth_token`, `slack_webhook`), `aws_access_key` widened to `ASIA`/`ABIA`/`ACCA`, `pem_private_key` extended to PGP blocks, `ai_provider_key` renamed `api_key_sk` and widened, `phone_in` accepts a 5+5 spaced grouping - 15 patterns -> 18; the always-skipped path list gained `node_modules`/`vendor`/`site-packages`/`.gradle`/`.idea`/`.vscode`/`.mypy_cache`, lockfile names, and `.map`/`.min.js`/`.min.css`/`.lock`/`.svg` suffixes (deliberately still not skipping `dist`/`build`/`out`/`target`/`coverage`, and no path escape marker - see research.md D1). `.claude/settings.json`'s hook command now reads `${CLAUDE_PROJECT_DIR:-.}` and ends `|| exit 0`, so an unset `$CLAUDE_PROJECT_DIR` degrades to allow instead of crashing `python3` and fail-closing every write in the session. `.scanignore`: `Director Name` removed (no pattern can ever match it); the three original false-positive entries (a template hash, a bash int64-max constant) removed now that their root cause is fixed at the pattern level; no new entries: the hex-hash substrings that the digit-only boundary let through were fixed at the pattern with alphanumeric boundaries on `phone_us`, `phone_in` and `payment_card`.specify/integrations/speckit.manifest.json` (a Spec Kit checksum manifest committed before this feature existed): the exact bracketing above closes the digit-run-inside-a-digit-run class but, as an unavoidable side effect of that literal bracketing, opens a digit-run-inside-an-alphanumeric-run class neither `phone_us` nor `payment_card` has a checksum to guard against; flagged as a deviation from a "six fixtures only" instruction rather than silently resolved either direction - see `.scanignore`'s own comment. Unit suite: 201 passed, 7 skipped (opt-in browser suite) in about 3.6 s. `--history` on the real repo: exit 0, under 0.4 s (system `python3` 3.9.6) and under the 2 s budget. |
| 2026-08-24 | UPDATE | `scripts/check_env.py`, `tests/test_check_env.py`, `scripts/README.md`, `CLAUDE.md`, `.specify/memory/constitution.md`, `PATTERNS.md`, `README.md`, `MEMORY.md`, `Project_Structure.md` | **V0.0.2 Commit Safety Gate, docs of record.** `scripts/check_env.py` gained the `git_hooks` row (PASS when `core.hooksPath` reports `.githooks`, FAIL with the activation command) and its `ConfigError` early-exit path now SKIPs every row but `vault` generically instead of a hardcoded row count; `tests/test_check_env.py` gained matching PASS/FAIL/git-missing tests. `scripts/README.md` gained a Maintenance row for `scan_secrets.py`. `CLAUDE.md` Lesson 4 names the scanner and the mandatory `.githooks` activation. `constitution.md` bumped to 1.2.0 with a Sync Impact Report line, Principle IV, and a new "Public repository hygiene" Hard Rules bullet. `PATTERNS.md` gained the "Commit safety gate (v0.0.2)" entry (design notes plus the four empirically-discovered false positives). `README.md` gained a Setup activation step and a "Public repo hygiene" section. `MEMORY.md` gained one line under "Identity and environment" recording the gate's activation requirement. This table (Director-layer rows for `.githooks/`, `.github/`, `.scanignore`, an updated `.claude/` description, and an `Application Layer` row for `scan_secrets.py`) is this same change. |
